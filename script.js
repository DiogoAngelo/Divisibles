import PrintDivisibles from "./print-divisibles.js";

// The first parameter should be the maximum number.
// The second parameter should be the first divisible.
// The third parameter should be the second divisible.

const divisibles = new PrintDivisibles(100, 3, 5);
divisibles.init();
